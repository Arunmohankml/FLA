import { BookDemoClient } from "./BookDemoClient";

export const metadata = {
  title: "Book a Demo at Foreign Language Academy",
  description:
    "Book a free demo with Foreign Language Academy in Chennai and get expert guidance on the right language course, level, and batch.",
};

export default function BookDemoPage() {
  return <BookDemoClient />;
}
